<%= form_with(model: salesproduct, local: true) do |form| %>
  <% if salesproduct.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(salesproduct.errors.count, "error") %> prohibited this salesproduct from being saved:</h2>

      <ul>
      <% salesproduct.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :unit %>
    <%= form.number_field :unit %>
  </div>

  <div class="field">
    <%= form.label :total_price %>
    <%= form.text_field :total_price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
